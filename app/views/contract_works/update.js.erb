<% if @work.valid? %>
$("#contract-works").replaceWith("<%= j render partial: 'versions/contract_works_list' %>");
$("#work-form-row-<%= @work.id %>").remove();
<% else %>
$("#work-form-row-<%= @work.id %> td").html("<%= j render('form') %>");
<% end %>